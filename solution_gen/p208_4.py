

import math

n = [int(x) for x in input().split(' ')]

def sos(n, step):
    angle_deg = 360.0 / n
    print('angle_rot', step * angle_deg)
    if step == 0
        return step
    length = length_of_curve_1()
    return length * math.tan(math.radians(angle_deg))

def length_of_curve_1():
    return math.sqrt(pow(length_1, 2) + pow(length_2,2))

print(sos((n[0]), (n[1])))